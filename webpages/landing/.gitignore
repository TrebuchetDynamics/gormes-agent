# ── Legacy Go build outputs ──────────────────────────
bin/
*.exe
*.dll

# ── Astro static export (rebuilt every deploy) ───────
dist/
.astro/

# ── Node / Playwright ────────────────────────────────
node_modules/
playwright-report/
test-results/
playwright/.cache/
.playwright-cache/

# ── Agent session scratch (claude-flow, swarm, etc.) ─
.claude-flow/
.swarm/
.superpowers/

# ── Transient deploy trigger markers ─────────────────
.deploy-trigger

# ── Progress SSoT mirror (checked in; root .gitignore excludes data/) ─
!src/data/
!src/data/benchmarks.json
!src/data/progress.json
