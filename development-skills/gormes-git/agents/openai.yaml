interface:
  display_name: "Gormes Git"
  short_description: "Commit and push development branch work."
  default_prompt: "Use $gormes-git to inspect the dirty development worktree, reconcile generated surfaces, validate, split coherent commits, and push origin development when requested."
