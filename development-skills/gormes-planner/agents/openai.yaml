interface:
  display_name: "Gormes Planner"
  short_description: "Turn parity gaps and decision=plan handoffs into buildable rows."
  default_prompt: "Use $gormes-planner to rerun selectors, shape one source-backed gap through typed progress tooling, keep unresolved filesystem/process/network/secret policy draft, prove repo-only selection, and never implement runtime code."
