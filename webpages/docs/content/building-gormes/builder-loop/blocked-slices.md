---
title: "Blocked Slices"
weight: 40
aliases:
  - /building-gormes/blocked-slices/
---

# Blocked Slices

This page is generated from canonical `progress.json` rows that declare
`blocked_by`.

Use it to avoid assigning work before the dependency chain is ready.

<!-- PROGRESS:START kind=blocked-slices -->
| Phase | Slice | Blocked by | Ready when | Unblocks |
|---|---|---|---|---|
| 5 / 5.C | Goscrapling local crawler adapter gate for web_crawl | Goscrapling v0.1.x robots/cache/checkpoint release gate, Robots.txt manager and delay directives, Development response cache, Checkpoint pause and resume, Spider static, dynamic, stealth, and proxy session adapters | Gormes' current Firecrawl/Tavily `web_crawl` adapter and URL/website-policy safety tests remain complete and green., The public `github.com/TrebuchetDynamics/goscrapling` module version used by Gormes resolves from the Go module graph with no local `replace` and exposes crawler/session/result APIs plus tests equivalent to the cited v0.1.0 spider foundation., A goscrapling release or verified dependency update closes the active robots, response-cache, checkpoint, and session-adapter blockers, or this row is deliberately narrowed to exclude each still-missing primitive with explicit degraded evidence., A hermetic httptest/fake-session fixture can prove crawl discovery, allowed-domain/offsite behavior, robots allow/deny, max pages, duplicate skipping, cancellation/degraded evidence, and result normalization without live network, browser, provider, or LLM credentials., The product decision remains that local goscrapling crawling is an operator-selected backend behind `web_crawl`, not an automatic replacement for provider crawl APIs and not a new model-visible tool name. | Local no-provider research crawls, Crawler-backed research workflows on Termux and locked-down corp Linux, Hermes tool tail strict-fidelity source-pair expansion |
| 5 / 5.O | Navivox multi-server profile routing config model | Root config.toml v2 profile service schema, Per-profile channel credential readiness and allow-lists | Root config.toml v2 profile service schema exists., Per-profile channel credential/readiness rows provide the profile channel model Navivox references., Tests use fake server/config routing and never start live HTTP, WS, VPN, SSH, STT, or TTS processes. | - |
| 5 / 5.O | Legacy profile config v2 migration planner | Root config.toml v2 profile service schema | Root config.toml v2 profile service schema is complete or the migration tests can construct the same target structs without command wiring., Sample legacy profile roots are represented by tempdir fixtures with no real operator secrets. | Profile Control Center read model, Profile Control Center TUI shell and draft apply flow |
| 5 / 5.O | Profile Control Center read model | Root config.toml v2 profile service schema, Legacy profile config v2 migration planner | Root config v2 structs exist or are faked behind a test seam., Legacy migration planner can be injected or represented by a fake warning source. | Profile Control Center TUI shell and draft apply flow |
| 5 / 5.O | Profile Control Center TUI shell and draft apply flow | Profile Control Center read model | Profile Control Center read model is complete and exposes typed rows/actions., The draft writer can target config v2 and migration planner APIs through injected seams. | - |
| 5 / 5.O | Per-profile provider credential readiness | Root config.toml v2 profile service schema | Root config.toml v2 profile service schema exists., Provider registry/model picker seams are fakeable; no live OpenRouter/OpenAI/Anthropic call is required for tests. | Profile Control Center read model, Profile Control Center TUI shell and draft apply flow |
| 5 / 5.O | Per-profile channel credential readiness and allow-lists | Root config.toml v2 profile service schema | Root config.toml v2 profile service schema exists., Gateway/channel config tests can use synthetic profile-channel configs and fake credentials without starting real platform clients. | Profile Control Center read model, Profile Control Center TUI shell and draft apply flow, Navivox multi-server profile routing config model |
| 8 / 8.C | Engineering writeup #1: autonomous Hermes-porting loop | TD engineering blog scaffolded and live, Loop $/iteration cost metric in status file | TD blog (8.A row 1) is live., Loop $/iteration cost telemetry (8.F) has at least one week of data., Operator has decided the publication date and platform (HN/Lobsters/Reddit). | Engineering writeup #2: validation-gated agentic engineering, Engineering writeup #3: Gormes vs Hermes-Python benchmarks, HN launch post for Gormes 1.0 |
| 8 / 8.D | Single-binary cross-platform release pipeline | Termux latest-installer follow-up release publication, Removal of public v0.2.20 Termux latest-install caveats from README, landing, install docs, and troubleshooting docs | Sharp v1.0 differentiator (8.D row 1) is decided., go build ./cmd/gormes succeeds on all seven target GOOS/GOARCH combinations from CI or a Linux runner where cross-compilation is supported., All release child rows, including `Termux latest-installer follow-up release publication` and `Removal of public v0.2.20 Termux latest-install caveats from README, landing, install docs, and troubleshooting docs`, are complete or explicitly superseded by release-lane evidence. | - |
| 8 / 8.D | Termux latest-installer follow-up release publication | explicit release-lane approval for Termux installer follow-up | Operator explicitly approves a release-lane action for the Termux installer recovery., origin/development contains fix commit 72b4ee248475 or a superseding Termux installer recovery commit., The development branch is clean enough for the release skill to include or split all intended dirty work safely., A concrete follow-up tag name is selected (default v0.2.21 unless superseded) and remote tag/release checks confirm it is available for creation or recovery., The gormes-release preflight can fetch origin main/development/tags and verify gh authentication before opening or updating the development -> main release PR. | Single-binary cross-platform release pipeline, Removal of public v0.2.20 Termux latest-install caveats from README, landing, install docs, and troubleshooting docs |
| 8 / 8.D | Removal of public v0.2.20 Termux latest-install caveats from README, landing, install docs, and troubleshooting docs | Termux latest-installer follow-up release publication | The `Termux latest-installer follow-up release publication` row is complete or a superseding release row records the public tag, release URL, artifact, and latest-install evidence., gh release evidence confirms the follow-up tag is public, non-draft, non-prerelease, and contains install.sh plus the android-arm64 artifact., The selected follow-up tag/version/date alias is known so README, `cmd/gormes/version.go`, and `webpages/landing/src/data/release.json` can move together., The existing caveat-presence tests are ready to be inverted or renamed into stale-v0.2.20-regression tests rather than deleted without replacement. | Single-binary cross-platform release pipeline |
| 9 / 9.F | Navivox Telegram-inspired chat polish | Navivox connect-and-talk first screen, Navivox profile contact summary API | `Navivox connect-and-talk first screen` has landed and provides a live chat fixture., `Navivox profile contact summary API` has landed so the list uses server-authoritative profile contacts, not mocks., The UI docs classify Telegram/TDLib references as rendering/lifecycle inspiration only, not backend dependencies., The package/dependency review for `v_chat_bubbles` can be performed without blocking text chat. | - |
| 9 / 9.F | Navivox natural-language profile seed flow | Navivox profile contact summary API | `Navivox profile contact summary API` has landed so newly seeded profiles can appear as contacts immediately., Existing `gormes profile` create/list/show/use behavior remains green and must not be replaced., The design preserves Gormes' server-authoritative config model; the Flutter app requests creation and renders/edit drafts, it does not write TOML. | Navivox per-profile BYO voice profiles |
| 9 / 9.F | Navivox structured tool event cards | Navivox connect-and-talk first screen | `Navivox connect-and-talk first screen` has landed so a streaming chat fixture exists., The gateway manager already has render-frame tool progress and channel-neutral fallback behavior., Flutter already has a basic NavivoxToolCall model and ToolCall body renderer to extend. | - |
| 9 / 9.F | Navivox safe config admin over HTTP | Navivox HTTP/WS documentation refresh, Navivox connect-and-talk first screen | `Navivox HTTP/WS documentation refresh` has landed so config admin docs describe HTTP/WS events, not stdio frames., `Navivox connect-and-talk first screen` has landed so the app has a live authenticated gateway connection., The config schema must be generated from Gormes config metadata or a typed allowlist; no free-form TOML editor. | Navivox per-profile BYO voice profiles |
| 9 / 9.F | Navivox voice run records | Navivox connect-and-talk first screen, Navivox continuous voice command mode | `Navivox connect-and-talk first screen` has landed and produces session ids for normal turns., `Navivox continuous voice command mode` has landed or its cancellation lifecycle contract is explicit., `Navivox structured tool event cards` is at least planned so tool timeline data has a typed event shape., The row uses existing session/store/apiserver surfaces where possible instead of creating a side database. | Navivox per-profile BYO voice profiles |
| 9 / 9.F | Navivox per-profile BYO voice profiles | Navivox natural-language profile seed flow, Navivox safe config admin over HTTP, Navivox voice run records | `Navivox natural-language profile seed flow` has landed so profiles have editable draft metadata., `Navivox safe config admin over HTTP` has landed so provider/secret changes use server-side validation and write-only secrets., `Navivox voice run records` has landed or is in progress so STT/TTS evidence can be audited. | - |
<!-- PROGRESS:END -->
