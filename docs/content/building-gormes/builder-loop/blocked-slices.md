---
title: "Blocked Slices"
weight: 40
aliases:
  - /building-gormes/blocked-slices/
---

# Blocked Slices

This page is generated from canonical `progress.json` rows that declare
`blocked_by`.

Use it to avoid assigning work before the dependency chain is ready.

<!-- PROGRESS:START kind=blocked-slices -->
| Phase | Slice | Blocked by | Ready when | Unblocks |
|---|---|---|---|---|
| 4 / 4.A | Codex OAuth state + stale-token relogin | Token vault, Multi-account auth, Codex Responses pure conversion harness, Codex Responses assistant content role types, Codex Responses HTTP client binding | Gormes has an XDG-scoped token vault and account-selection seam for provider credentials. | - |
| 4 / 4.B | Kernel compression-boundary callback binding | ContextEngine compression-boundary callback vocabulary, Context compression | ContextEngine compression-boundary callback vocabulary is complete on main., The worker identifies the existing kernel compression execution point; if no explicit compression execution path exists in the checkout, keep this row blocked and do not create fake wiring., A fake ContextEngine can record boundary notifications without calling a live provider or mutating real transcript storage. | - |
| 4 / 4.F | Auto-naming sessions | Title prompt and truncation contract, Title auxiliary failure visibility | Title prompt and truncation contract and Title auxiliary failure visibility are validated on main., Session metadata write helpers can be exercised with temp stores; no live provider or TUI program is required. | - |
| 4 / 4.G | Anthropic OAuth/keychain credential discovery | Token vault | Token vault owns XDG-scoped credential files and can expose provider auth status without live credentials. | - |
| 4 / 4.G | Google OAuth flow + refresh seam | Token vault | Token vault is planned but not required for this pure state-machine slice; tests inject token/client-secret paths under t.TempDir., The worker can model OAuth state and token payloads without importing Google SDKs, opening a browser, making HTTP requests, or touching provider adapters., The row is about Google Workspace skill auth and future Google provider setup, not Google Code Assist model routing. | - |
| 5 / 5.B | Terminal snapshot source stdout suppression guard | Environment interface + file sync contract | Environment interface + file sync contract has landed and exposes a shell-wrapper construction seam before backend-specific execution is enabled., Tests can use string-built shell wrapper fixtures and fake snapshots; no real shell, macOS bash, Docker, SSH, or sandbox backend is required., The row is about terminal environment wrapper construction only, not execute_code or durable worker shell jobs. | - |
| 5 / 5.B | Raw tool-call parser fixture matrix | Environment interface + file sync contract | Environment interface + file sync contract has established the parser-family inventory and fake fixture harness., The first parser row can remain a manifest and golden-output fixture package with no live provider calls. | OpenRouter, Gemini, Google Code Assist |
| 5 / 5.D | Image-too-large shrink retry helper | Provider image-too-large error classification, Image input mode router + native content parts | Provider image-too-large error classification is validated so retry planning can key off a structured image_too_large kind., Image input mode router + native content parts is validated or fixture-ready, so native image content-part shape is known., The first slice may use a deterministic fake shrinker callback in tests; real image decoding/resizing can stay out of scope. | - |
| 5 / 5.D | Image generation result contract | Image input mode router + native content parts | Image input mode router + native content parts is fixture-ready or complete, so multimodal artifact vocabulary is no longer ambiguous., The row defines result schema only; workers can use fake binary bytes and temp output directories without provider calls. | - |
| 5 / 5.F | Skills hub direct URL install name/category guard | Skills hub direct URL candidate parser, SKILL.md frontmatter validation guard | Skills hub direct URL candidate parser is validated and returns stable missing-name/invalid-name evidence., SKILL.md frontmatter validation guard is validated so malformed URL skills are excluded before prompt injection., The worker can use an injected fetcher and fake quarantine/scan result; no live HTTP, GitHub registry, or prompt-selection runtime is required. | - |
| 5 / 5.I | First-party Google Meet plugin metadata fixture | Plugin SDK, First-party Spotify plugin fixture | Plugin SDK and First-party Spotify plugin fixture are validated on main, so disabled first-party plugin metadata and tool schemas already have a pattern., The row is metadata-only: workers parse static upstream files or local fixtures and emit disabled inventory, with no meet bot, Chrome, audio bridge, OpenAI Realtime client, node server, or process manager execution., Safety evidence must record explicit URL-only join behavior and one-active-meeting/runtime requirements without claiming calendar scanning or automatic consent announcements. | Google Meet process/node lifecycle contract, Meeting transcript memory handoff contract |
| 5 / 5.I | Hindsight memory setup blank-input preservation | Third-party extensions | Third-party extensions have a native plugin config-patch seam; do not implement this before the generic plugin setup surface exists., Tests can exercise a pure config patch helper with in-memory maps and temp files; no Hindsight cloud/local daemon, uv install, Python plugin import, or Goncho memory change is required., This row is external memory-plugin compatibility only; internal Goncho remains the Go-native Honcho-compatible memory implementation. | - |
| 5 / 5.J | Subagent dangerous-command non-interactive approval policy | Recoverable dangerous patterns + blocked-result schema, Approval mode config normalization | Dangerous-command detection and approval-mode config normalization are fixture-locked for local tools. | - |
| 5 / 5.J | Concurrent tool approval callback propagation | Recoverable dangerous patterns + blocked-result schema, Subagent dangerous-command non-interactive approval policy | Recoverable dangerous patterns + blocked-result schema is validated, so approval decisions can reuse GuardCommand evidence., Subagent dangerous-command non-interactive approval policy is fixture-ready or validated; this row focuses on concurrent tool worker callback inheritance and fail-closed fallback., Tests use fake callbacks and fake worker functions only; no real terminal, prompt toolkit, shell command, or subagent backend is required. | Background review toolset restriction |
| 5 / 5.N | Debug share paste sweep scheduler contract | Debug helpers | Debug helpers are split into a Go-native diagnostic surface with redaction and no hidden writes., A native debug-share pending-paste queue exists or is specified as a pure helper before scheduler wiring begins., Tests can use fake clock and fake deleter; no network DELETE, paste.rs upload, gateway process, or live cron daemon is required. | - |
| 5 / 5.N | Cron run resource release executor binding | Cron run resource release contract | Cron run resource release contract is complete on main and run_release.go exposes the typed ledger., The worker can use fake KernelAPI, fake DeliverySink, and fake LiveDeliveryAdapter values; no real SQLite open, real provider HTTP, real MCP stdio runtime, or real Scheduler goroutine is required., ExecutorConfig may grow only the minimum dependency-injection surface (a release ledger factory or a Closer/PID register hook) needed for tests; no schedule-engine, kernel, store, or run-completion semantics are in scope. | - |
| 5 / 5.O | Gormes config command surface | Hermes CLI command-tree parity manifest, Provider endpoint/API-key root flags + runtime resolution | The provider endpoint/API-key flag row has established runtime precedence and redaction vocabulary., The first implementation can support the current Gormes schema only: hermes.endpoint, hermes.model, gateway.*, tui.*, input.*, telegram.*, discord.*, slack.*, cron.*, skills.*, delegation.*, and goncho.*., Config writes may use an internal writer helper; no setup wizard or provider validation prompt is required. | Gormes config edit/check/native schema-migrate closeout, Hermes config migration writer, OpenClaw migration writer and cleanup command |
| 5 / 5.O | Gormes config edit/check/native schema-migrate closeout | Gormes config command surface, Hermes CLI command-tree parity manifest | Gormes config command surface is complete for show/path/env-path/set and owns the writer helpers., The Hermes CLI command-tree parity manifest classifies config edit/check/migrate as row-backed until this closeout lands., The first native migrate implementation may be a no-op when CurrentConfigVersion is already current, but it must still report version and missing/default status deterministically. | Hermes config migration writer, OpenClaw migration writer and cleanup command |
| 5 / 5.O | Hermes config migration dry-run manifest | Gormes config command surface, Hermes CLI command-tree parity manifest | The Gormes config command row defines the target config/dotenv write contract., This slice can be implemented as read-only manifest generation with temp fixture directories., State import for sessions/memory can remain archived/skipped in this dry-run row. | Hermes config migration writer |
| 5 / 5.O | Hermes config migration writer | Hermes config migration dry-run manifest, Gormes config command surface, Gormes config edit/check/native schema-migrate closeout | Hermes config migration dry-run manifest is complete., Gormes config command surface is complete and owns target-file writer semantics., The first writer slice is allowed to import config/dotenv only; memory/session migration requires dependent rows. | - |
| 5 / 5.O | OpenClaw migration dry-run manifest | Gormes config command surface, Hermes CLI command-tree parity manifest | Gormes config command surface defines the target config/dotenv write contract., The manifest row is read-only and can use compact JSON fixtures rather than a real OpenClaw install., The command name is `gormes migrate openclaw`; the misspelled `ooenclaw` is not accepted as an alias and must produce a user-facing suggestion for `gormes migrate openclaw`. | OpenClaw migration writer and cleanup command |
| 5 / 5.O | OpenClaw migration writer and cleanup command | OpenClaw migration dry-run manifest, Gormes config command surface, Gormes config edit/check/native schema-migrate closeout | OpenClaw migration dry-run manifest is complete., Gormes config command surface is complete and owns target-file writer semantics., The writer applies only manifest-backed items; any unmapped OpenClaw surface remains archived/manual with report evidence. | - |
| 5 / 5.P | Unix installer root/FHS layout policy | Unix installer (install.sh) source-backed update flow | Unix installer (install.sh) source-backed update flow has canonical scripts under scripts/ and a byte-equal served site copy. | Installer site asset/route coverage |
| 5 / 5.Q | Native TUI Hermes status bar renderer | Native TUI Hermes skin token renderer | Native TUI Hermes skin token renderer is complete so status bar style tokens are available., Tests can construct synthetic kernel.RenderFrame values and fixed timestamps; no Bubble Tea program, provider, or real terminal is needed. | Native TUI Hermes bottom-pinned chrome layout |
| 5 / 5.Q | Native TUI Hermes bottom-pinned chrome layout | Native TUI renderConv viewport budget binding, Native TUI Hermes skin token renderer, Native TUI Hermes status bar renderer | Native TUI renderConv viewport budget binding is complete so conversation tail rendering is bounded., Native TUI Hermes skin token renderer and status bar renderer are complete., The worker may edit cmd/gormes/main.go only to remove/adjust alt-screen program options and must keep --offline local-smoke behavior intact. | Native TUI Hermes input keybinding semantics, Native TUI Hermes tool progress + modal panel renderers |
| 5 / 5.Q | Native TUI Hermes input keybinding semantics | Native TUI Hermes bottom-pinned chrome layout | Native TUI Hermes bottom-pinned chrome layout is complete or the worker can test key handling independently without depending on exact view order., internal/cli.CommandRegistry active-turn policy remains the source of slash known/unavailable/queue/bypass behavior., Queued-message helper may be incomplete; this row should return explicit unavailable evidence for unported queue/steer behavior unless those dependent rows have landed. | Native TUI queued-message delete/cancel binding, TUI queued submit dispatch while in-flight, Native TUI Hermes slash completion helpers |
| 5 / 5.Q | Native TUI Hermes slash completion helpers | Native TUI Hermes input keybinding semantics | internal/cli.CommandRegistry is validated against Hermes slash command inventory or carries explicit unavailable rows for missing commands., Pure helper tests can run without a Bubble Tea Program or terminal. | Native TUI Hermes completion menu binding |
| 5 / 5.Q | Native TUI Hermes tool progress + modal panel renderers | Native TUI Hermes bottom-pinned chrome layout | Native TUI Hermes bottom-pinned chrome layout is complete so panel placement has a target stack., At least synthetic panel state types can be added under internal/tui without requiring live terminal callbacks, tool execution, provider calls, or dangerous-command approval plumbing., If kernel/tool event state is insufficient for runtime wiring, this row should remain renderer-only and add a follow-up interface-design row. | - |
| 5 / 5.Q | API server cron admin read-only endpoints | API server detailed health endpoint | API server detailed health endpoint is complete., Cronjob tool action envelope over native store, Cron context_from output chaining, and Cron multi-target delivery + media/live-adapter fallback are validated on main., Tests can seed the native cron store/run audit directly; no scheduler goroutine, live delivery adapter, provider, or cron mutation endpoint is required. | API server cron admin mutating endpoints |
| 5 / 5.Q | API server cron admin mutating endpoints | API server cron admin read-only endpoints | API server cron admin read-only endpoints is complete and exposes the same cron store/read model., Cron prompt/script safety + pre-run script contract, Cronjob tool action envelope over native store, and Cron multi-target delivery + media/live-adapter fallback are validated on main., Tests can inject fake delivery/action handlers; no live gateway adapter, provider, or wall-clock scheduler tick is required. | - |
| 6 / 6.B | LLM-assisted pattern distillation | Portable SKILL.md format | Portable SKILL.md format is validated, so generated drafts can carry review state and remain out of prompt injection until approved., The row can use fake model/review prompt fixtures; no live provider, skill install, or prompt injection is required. | - |
| 6 / 6.C | Portable SKILL.md format | SKILL.md frontmatter validation guard, Hermes creative skill metadata compatibility, Phase 2.G skills runtime | SKILL.md frontmatter validation guard is validated so malformed skill files are excluded before review metadata is interpreted., Hermes creative skill metadata compatibility is validated so versioned metadata preserves real upstream skill routing rather than an invented generic design category., Phase 2.G skills runtime is complete and the parser/store seam is stable enough for versioned metadata. | LLM-assisted pattern distillation, Hybrid lexical + semantic lookup, Skill effectiveness scoring |
| 6 / 6.D | Hybrid lexical + semantic lookup | Portable SKILL.md format | Portable SKILL.md format is validated so skills carry review state, triggers, exclusions, and provenance metadata., Phase 3 semantic embedding lookup remains optional and can be stubbed in unit fixtures without Ollama. | Source-aware retrieval damping fixtures, Code Cathedral II code-context retrieval fixtures, Skill effectiveness scoring |
| 6 / 6.D | Source-aware retrieval damping fixtures | Hybrid lexical + semantic lookup | Hybrid lexical + semantic lookup is validated and returns score explanations that can carry source-tier evidence., The worker can use synthetic skill/memory sources and fake scores; no GBrain SQL engine, PGLite, Postgres, or tree-sitter runtime is required. | Delta-bounded skill and memory maintenance passes, Code Cathedral II code-context retrieval fixtures, Skill effectiveness scoring |
| 6 / 6.D | Delta-bounded skill and memory maintenance passes | Source-aware retrieval damping fixtures | Hybrid lexical + semantic lookup and Source-aware retrieval damping fixtures are validated so maintenance evidence can reuse score explanations and source tiers., The worker can use fake skill/memory rows plus injected source-anchor and stale-count callbacks; no live embeddings, Ollama, Postgres/PGLite, git cat-file, or tree-sitter runtime is required. | Skill effectiveness scoring |
| 6 / 6.D | Code Cathedral II code-context retrieval fixtures | Hybrid lexical + semantic lookup | Hybrid lexical + semantic lookup is validated and can accept optional external evidence in fixtures. | - |
| 7 / 7.C | Matrix self/bridge sender drop helper | Matrix shared-chassis bot seam | Matrix shared-chassis bot seam exists so the helper can live under internal/channels/matrix without inventing a parallel gateway adapter., The slice is pure string classification plus table tests; no live Matrix sync, login, pairing store, E2EE store, room join, or gateway manager behavior is required., Tests should cover sender localpart parsing with Matrix IDs only; no mautrix/nio SDK dependency is needed. | Matrix real client/bootstrap layer |
| 7 / 7.C | Matrix E2EE device-id crypto-store binding | Matrix real client/bootstrap layer | Matrix real client/bootstrap layer has auth, sync/invite handling, room-kind policy, and a fakeable E2EE bootstrap seam. | - |
| 7 / 7.E | Yuanbao media/sticker attachment normalization | Yuanbao protocol envelope + markdown fixtures | Yuanbao protocol envelope + markdown fixtures is fixture-ready or validated., The row can use static media/sticker metadata fixtures and t.TempDir files; no live download, CDN auth, or sticker decoding service is required., Gateway attachments already have a degraded evidence path for unavailable media. | Yuanbao gateway runtime + toolset registration |
| 7 / 7.E | Yuanbao gateway runtime + toolset registration | Yuanbao protocol envelope + markdown fixtures, Yuanbao media/sticker attachment normalization, Platform toolset config persistence + MCP sentinel | Yuanbao protocol envelope + markdown fixtures and Yuanbao media/sticker attachment normalization are fixture-ready or validated., Shared gateway manager registration and platform toolset config persistence are validated on main., The first runtime slice may use fake login/send clients only; live QR/session handling remains disabled until operator config and docs are present. | - |
<!-- PROGRESS:END -->
