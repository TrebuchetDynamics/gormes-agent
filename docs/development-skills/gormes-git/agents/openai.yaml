interface:
  display_name: "Gormes Git"
  short_description: "Commit, push, PR-merge development."
  default_prompt: "Use $gormes-git to validate, commit all dirty Gormes changes, push development, handle the development-to-main PR when requested, and report the resulting git state."
