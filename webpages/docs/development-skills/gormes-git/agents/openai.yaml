interface:
  display_name: "Gormes Git"
  short_description: "Session-safe sync, commit, push, PR-merge development."
  default_prompt: "Use $gormes-git to classify the requested git intent, preserve unrelated dirty work, reconcile generated progress/site surfaces when needed, validate, commit or push only the requested development-branch scope, and handle PR work only when asked."
