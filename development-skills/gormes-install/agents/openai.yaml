interface:
  display_name: "Gormes Install"
  short_description: "Validate or perform Gormes installs."
  default_prompt: "Use $gormes-install to either validate release install/setup paths in an isolated sandbox or, when explicitly requested, build and install the current development checkout on this PC with binary and gateway freshness checks."
