# Memory Identity Specs

Owns lattice memory, graph extraction, recall, semantic fusion, operational mirrors, and decay design decisions.

Exposes dated design specs for identity and memory behavior.

Must never know about UI rendering, channel transport details, or installer flow except through persisted/runtime contracts.
