---
title: "Blocked Slices"
weight: 40
aliases:
  - /building-gormes/blocked-slices/
---

# Blocked Slices

This page is generated from canonical `progress.json` rows that declare
`blocked_by`.

Use it to avoid assigning work before the dependency chain is ready.

<!-- PROGRESS:START kind=blocked-slices -->
| Phase | Slice | Blocked by | Ready when | Unblocks |
|---|---|---|---|---|
| 2 / 2.B.5 | Gateway session reset notification parity | Gateway manual reset session-boundary hooks, Session expiry hook cleanup retry evidence | Gateway manual reset session-boundary hooks are planned and the worker can keep explicit reset hooks separate from auto-reset notices., Session expiry hook cleanup retry evidence is complete, so auto-reset notification evidence can reuse existing session metadata and status seams without changing expiry finalization., Tests can use fake clocks, fake session metadata, fake channels, and fake notification senders only; no live Telegram, Slack, Discord, API server, webhook, provider, or SQLite production store is required. | Fresh-reset skill injection closeout, Channel session lifecycle status surface |
| 2 / 2.B.5 | Cross-platform multi-image native batching | Cross-platform image/document MEDIA delivery routing | Cross-platform image/document MEDIA delivery routing is implemented or being completed in the same dependent slice, so image files already reach a typed gateway media list., Tests can use temp files and fake Telegram, Discord, Slack, and email clients/builders; no live channel credentials, upload APIs, or SMTP server are required. | - |
| 2 / 2.B.5 | Discord message admission + reply-mode policy | Discord thread participation persistence | Discord forum channel ingress and SessionSource guild/parent/message metadata are complete, and the pending thread participation persistence row defines the known-thread tracker seam., Tests can use fake discordgo messages, fake channels/threads, fake client user IDs, and env/config values; no live Discord app, gateway socket, or REST call is required. | - |
| 2 / 2.B.5 | Webhook dynamic route reload + signed rate-limit order | Signed event parsing + auth gates, Prompt-to-delivery routing bridge | Signed event parsing + auth gates and Prompt-to-delivery routing bridge are complete, so the row can bind runtime request ordering without changing prompt rendering semantics., Tests can use httptest/fake clocks/temp Gormes home files and do not need public webhook exposure, live providers, or external network access. | - |
| 2 / 2.B.5 | Telegram fallback transport + polling reconnect recovery | Telegram polling conflict + webhook secret startup guard | Telegram polling conflict + webhook secret startup guard defines startup-state evidence and retryable/fatal classification., Tests can inject fake HTTP transports, fake polling request pools, fake clocks, and fake heartbeat probes; no live Telegram network or bot token is required. | - |
| 2 / 2.B.5 | Telegram sticker vision adapter binding | Mirror + sticker cache surfaces | Mirror + sticker cache surfaces is complete and provides Hermes-compatible cache lookup/injection helpers., Tests can inject fake Telegram GetFile/DownloadFile clients, fake vision analyzers, temp cache directories, and synthetic sticker updates; no live Telegram file download or vision provider is required. | - |
| 2 / 2.B.5 | Discord native slash/thread command registration parity | Discord interaction authorization + mention safety guards, Slack/Discord channel-scoped skills, prompts, and reload resync | Discord interaction authorization + mention safety guards and Slack/Discord channel-scoped skills, prompts, and reload resync define the auth and catalog seams this row consumes., Tests can use fake Discord command trees, fake plugin/skill command collectors, fake interactions, fake channels/threads, and fake ephemeral responses; no live Discord app command sync is required. | - |
| 2 / 2.B.5 | Telegram entity-only mention boundary closeout | Telegram group bot-command mention gate helper, Telegram group require-mention bot binding | Telegram group bot-command mention gate helper and Telegram group require-mention bot binding are complete; this row hardens their entity matching instead of changing the config vocabulary., Tests can synthesize tgbotapi.MessageEntity values and fake bot IDs; no live Telegram getMe or group traffic is required. | - |
| 2 / 2.B.5 | Telegram semantic MarkdownV2 formatter + table rewrite | Telegram MarkdownV2 parse-mode rendering closeout | Telegram MarkdownV2 parse-mode rendering closeout is complete, so Bot.Send/Edit already tells Telegram to parse MarkdownV2 and has parse-error fallback., Tests can call pure formatter helpers and fake Bot.Send chunking; no live Telegram parser or network is required. | - |
| 2 / 2.B.5 | Gateway platform reconnect isolation + channel health limits | Channel lifecycle writers into status model | Channel lifecycle writers into status model is complete, so reconnect/failure evidence can flow into the existing runtime status store without inventing a new status file., Tests can use fake channels/adapters, fake clocks, temp runtime stores, and injected HTTP limit helpers; no live Telegram/Discord/Slack/WhatsApp/HTTP clients are required. | - |
| 4 / 4.B | Manual compression feedback renderer + focus parser | ContextEngine interface + status tool contract, Compression token-budget trigger + summary sizing, Context references stable-handle store | ContextEngine interface, compression token-budget rows, and context references stable-handle store are complete, so this helper can remain a pure renderer/parser without running summarization., The worker can test with synthetic message-count/token-count structs and command strings only; no provider, kernel turn, summarizer, transcript store, TUI, channel, or context-reference expansion is required., This slice replaces the manual-feedback portion of the stale `Manual compression feedback + context references` placeholder and leaves live context-reference expansion to a separate future row. | CLI /compress runtime binding, Native TUI /compress command binding, Manual context-reference live expansion row |
| 4 / 4.C | Progressive subdirectory hint tracker | Context-file discovery + injection scan | Context-file discovery + injection scan is complete, so the implementation can reuse the existing scan/truncation behavior instead of duplicating prompt-injection policy., The worker can test with t.TempDir project trees and synthetic tool call descriptors; no real tool execution, provider call, model call, channel, TUI, or live repository scan is required., This row adds the pure tracker only; appending hints to actual tool results after tool execution remains a separate binding row. | Tool-result subdirectory hint injection binding, Subagent tool-result context hint parity, Cron workdir project-context parity |
| 4 / 4.D | Model catalog cache + preferred-provider live merge | Model pricing/capability registry fixtures, Gormes model interactive provider/model picker, Hermes provider registry and alias manifest | Provider-enforced context resolver, model pricing/capability registry fixtures, Hermes provider registry/alias manifest, and model picker command rows are complete., Tests can inject catalog JSON, fake fetchers, temp XDG cache paths, and fake provider credential presence; no live models.dev, Vercel AI Gateway, OpenRouter, Nous, or opencode-go network calls are required., The row owns catalog/cache/merge read models only; runtime routing and provider auth stay in separate rows. | - |
| 4 / 4.K | Hermes fallback activation + classifier carve-outs | Resilient provider chain dispatch, Hermes provider registry and alias manifest, Smart Model Routing, Context compressor provider-cap boundary | Provider registry and alias manifest can resolve fallback providers such as openrouter, zai, kimi-coding, minimax, openai-codex, and nous without live credentials., Existing provider fallback chain and model-router helpers are complete, so this slice can adapt loop activation without inventing a second fallback dispatcher., Context compressor budget helpers expose an update path for the active provider/model after route changes. | Provider-tool-memory golden transcript suite, Release readiness e2e gate |
| 5 / 5.B | Terminal deleted-cwd recovery guard | Native file task tool surface, Terminal cwd config bridge | Native file task tool surface and Terminal cwd config bridge are complete, so terminal execution already has a configured Workdir seam., Tests can use temp directories removed after setup plus fake command runners; no persistent shell, PTY, provider, or background process registry runtime is required. | - |
| 5 / 5.E | Voice mode environment detector + audio provider seam | Transcription tool contract, TTS tool contract + media delivery seam | Transcription tool contract, TTS tool contract + media delivery seam, and the existing VoiceModeRunner tests are complete., The worker can test the detector with injected env/proc-version/command/device probes and fake clocks; no real microphone, speaker, Termux app, PulseAudio, PortAudio, sounddevice, or numpy dependency is required., This slice does not need to bind native TUI record-key handling or gateway /voice command routing. | Voice record-key config binding for native TUI, TTS synthesis + voice-mode state |
| 5 / 5.E | Voice record-key config binding for native TUI | Transcription tool contract, TTS tool contract + media delivery seam | Transcription tool contract, Telegram voice/audio STT ingress hook, and TTS tool contract + media delivery seam are complete., Native TUI keybinding tests can resolve key strings without launching the Bubble Tea program, TTS providers, STT binaries, or a live gateway. | - |
| 5 / 5.F | Update bundled skills across active and named profiles | Skills list — enabled/disabled status column + --enabled-only filter | Skills list enabled/disabled status and bundled skill catalog rows are complete enough to enumerate reviewed bundled skills., Tests can build fake active and named profile roots under t.TempDir and inject bundled skill fixtures; no live update download, registry fetch, or real user profile mutation is required. | - |
| 5 / 5.G | MCP circuit breaker cooldown + reconnect reset | Managed tool gateway bridge, MCP OAuth refresh + 401 session-expired recovery, Gormes mcp login browser callback flow | MCP stdio/HTTP discovery, schema normalization, OAuth refresh, browser login, and managed gateway bridge rows are complete., The tool-call execution path has a fake session seam where tests can inject success/failure without live MCP servers or subprocesses., A fake clock can drive cooldown and half-open behavior deterministically. | Release readiness e2e gate, Long-running MCP server stability |
| 5 / 5.H | ACP JSON-RPC stdio session/prompt closeout | ACP server side, ACP Client Bridge Mode | ACP server side and ACP Client Bridge Mode are complete, so manifest, registry, permission-kind mapping, local client metadata, and session-key resolution are fixture-backed., The worker can test JSON-RPC over in-memory readers/writers and fake runtime/session stores; no editor, Python process, provider call, or terminal is required., Prompt execution can use an injected runtime seam that emits deterministic chunks, usage, errors, and cancellation evidence. | - |
| 5 / 5.H | ACP stdio benign ping/probe suppression | ACP JSON-RPC stdio session/prompt closeout | ACP server manifest and JSON-RPC stdio session/prompt closeout rows are complete enough to expose a Go ACP entrypoint seam., The worker can test with in-memory stdio buffers, fake JSON-RPC requests, and fake logger sinks only; no editor client, provider, MCP server, filesystem session store, or Python ACP runtime is required., This row owns entrypoint/log filtering only and does not implement missing ACP auth/session/tool surfaces. | ACP editor launch smoke parity, ACP startup diagnostics closeout |
| 5 / 5.I | Hermes plugin CLI lifecycle parity | Plugin SDK, Third-party extensions | Plugin SDK and discovery metadata are complete, so lifecycle commands can operate on manifest/status data without importing plugin code., Tests can use temp plugin roots, fake command runners, fake prompt input, and local fixture repositories without network access., The existing command-tree parity file advertises plugins subcommands; this row makes those commands real. | - |
| 5 / 5.I | Goncho Honcho plugin session config + async write compatibility | Goncho CRUD lifecycle invariants, Goncho Memory V1 compatibility contract and migration harness | Goncho topology design fixtures, CRUD lifecycle invariants, V1 memory compatibility, and empty peer-card hints are complete., Tests can use local Goncho service fakes, temp JSON config files, fake clocks, and fake async sinks; no hosted Honcho SDK, network, Redis, vector store, or background provider is required., Keep internal names Goncho while preserving public honcho_* compatibility and Honcho plugin config vocabulary. | - |
| 5 / 5.J | Email allowlist pre-dispatch loop guard | Email ingress + outbound delivery contract | Email ingress + outbound delivery contract is complete, so reply-target derivation and MIME/plaintext normalization already exist., Tests can use parsed RFC 822 messages and injected allowlist values without IMAP, SMTP, provider, or gateway manager startup. | - |
| 5 / 5.L | File write/patch staleness registry + cwd tracking | Native file task tool surface, Terminal cwd config bridge | Native file task tool surface is complete, including root-confined read_file, write_file, patch, and search_files., Terminal cwd config bridge is complete enough to provide a deterministic active cwd for relative path resolution., Tests can run entirely in t.TempDir with injected clocks and fake cwd/task IDs; no shell, provider, checkpoint, or live Hermes process is required. | Checkpoint restore tool parity, Terminal backend config parity, Safe multi-agent file editing |
| 5 / 5.L | search_files hidden-root and context-line parsing drift | Native file task tool surface | Native file task tool surface is complete and exposes SearchFilesTool over root-confined temp fixtures., The slice can exercise hidden directories and context-line parser helpers entirely under t.TempDir; no grep/ripgrep binary, live Hermes, or outside-workspace file read is required. | - |
| 5 / 5.M | Hermes Kanban worker tools and prompt gating | Hermes Kanban durable board core, Hermes Kanban dispatcher and worker spawn loop | Hermes Kanban durable board core is complete., Dispatcher row defines worker context env/state names or a fake worker context can be injected., Native tool registry supports contextual visibility and ownership-denied evidence. | Hermes Kanban slash/gateway/dashboard surfaces |
| 5 / 5.M | Hermes Kanban slash/gateway/dashboard surfaces | Hermes Kanban durable board core, Hermes Kanban dispatcher and worker spawn loop, Hermes Kanban production worker process binding, Hermes Kanban worker tools and prompt gating | Hermes Kanban durable board core is complete., Dispatcher and worker-tool rows define the status/read-model events the dashboard should render., Dashboard authentication and WebSocket/event-stream patterns are validated for existing Gormes dashboard routes. | - |
| 5 / 5.M | Hermes Kanban multi-board, workspace, and run-history parity | Hermes Kanban durable board core, Hermes Kanban dispatcher and worker spawn loop, Hermes Kanban production worker process binding | Hermes Kanban durable board core is complete., Dispatcher and worker-tool rows define run/log/heartbeat data that board views should expose., A Gormes-owned board namespace policy exists and rejects Hermes env aliases outside migrate commands. | - |
| 5 / 5.N | Cron script/workdir/inactivity execution binding | Cron prompt/script safety + pre-run script contract, Cronjob tool action envelope over native store | Cron prompt/script safety + pre-run script contract and Cronjob tool action envelope over native store are complete, so job fields are persisted and validated., Tests can inject script runners, fake clocks, fake provider streams, fake Codex refreshers, and temp stores; no real cron timer, shell, provider, or private config home is needed., This row intentionally precedes no-agent mode because no-agent depends on script execution being bound into the executor. | Cron no-agent script-only watchdog mode |
| 5 / 5.O | CLI terminal control-response sanitizer | CLI bracketed-paste wrapper sanitizer, CLI slow bracketed-paste diagnostic threshold | CLI bracketed-paste wrapper sanitizer and slow-paste diagnostic threshold rows are complete, so internal/cli already has pure input-normalization seams., The worker can use string literals for ESC, caret-escape, bare mouse, ANSI color, and normal text cases; no TTY, prompt_toolkit, mouse device, terminal emulator, or clipboard is required., This slice is helper-only and does not bind the sanitizer into Bubble Tea input loops or Cobra command dispatch. | CLI input-loop sanitizer binding, Native TUI mouse tracking input hygiene |
| 5 / 5.O | CLI submitted user-message preview formatter | CLI banner/output formatting helpers, CLI terminal control-response sanitizer | CLI banner/output helpers and terminal-control sanitizers are complete, so internal/cli already owns pure display-formatting helpers., The worker can use string fixtures and config structs only; no TTY, Bubble Tea model, prompt_toolkit, clipboard, provider, file-drop, image, or command dispatch is required., This slice is formatter-only and does not bind preview rendering into interactive CLI/TUI input loops. | CLI input-loop preview binding, Native TUI submitted-message preview parity, CLI prompt ergonomics closeout |
| 5 / 5.O | WhatsApp live Baileys QR pairing wizard | WhatsApp top-level pairing wizard shell | WhatsApp top-level pairing wizard shell is complete and tested., The implementation decides whether Gormes vendors, generates, or installer-fetches the bridge bundle with explicit provenance and without depending on `../hermes-agent` at runtime., Tests use fake node/npm/bridge fixtures and temp `GORMES_HOME`; no live WhatsApp QR scan or real session file is required. | WhatsApp gateway operator end-to-end pairing smoke, WhatsApp installed-runtime parity |
| 5 / 5.O | Self-update command lifecycle safety | Backup/update opt-in and exclusion policy, Service RestartSec parser helper, Service restart active-status poller, Gateway mutating-subcommand unavailability stub | Source-backed installer update_checkout logic is validated and can be reused or wrapped without changing curl-pipe install semantics., Backup/update opt-in policy, Service RestartSec parser helper, and Service restart active-status poller rows are complete., Tests can fake git, service-manager, dashboard-process, clock, signal, and log sinks; no network, root service manager, live gateway, or real checkout mutation is required. | Hermes CLI update parity, Installed-runtime self-healing smoke, Release readiness e2e gate |
| 5 / 5.O | Gateway planned stop marker + WSL systemd PATH parity | Runtime status JSON + PID/process validation, Gateway /restart command + takeover markers | Runtime status JSON + PID/process validation and Gateway /restart command + takeover markers are complete., The slice can use temp state roots, fake signal handlers, and pure service-unit string fixtures; no live systemd, WSL host, or real gateway process is required. | - |
| 5 / 5.Q | TUI gateway config health null-section probe | TUI gateway image/personality/platform-event helpers | TUI gateway image/personality/platform-event helpers are complete, so personality validation/rendering already exists as pure internal/tuigateway helpers., The worker can test decoded map[string]any fixtures directly; no config file read, TUI server, provider, SQLite session, SSE, Bubble Tea program, or HTTP transport is required., This row adds config-health and nil-tolerant session-option helpers only; runtime startup binding remains separate. | SSE streaming to Bubble Tea TUI, Remote TUI startup health surface |
| 5 / 5.Q | Native TUI clipboard, OSC52, and terminal setup parity | TUI TerminalNativeSelectionHelp constant + help-string fixture, Native TUI Hermes input keybinding semantics | TUI TerminalNativeSelectionHelp constant + help-string fixture and Native TUI Hermes input keybinding semantics are complete, so current non-copy behavior and keyboard semantics are locked., Tests can fake command execution, environment variables, terminal responses, and keybinding files without touching the host clipboard or user config., This row may add helper services and tests but must not wire copy/paste hotkeys into Update until the helpers are proven. | - |
| 5 / 5.Q | Native TUI image/file drop + paste collapse ingress | Native TUI Hermes input keybinding semantics, Native TUI clipboard, OSC52, and terminal setup parity, Native TUI absolute path completion routing | Native TUI Hermes input keybinding semantics, clipboard/OSC52 helper row, slash dispatch behavioral matrix, and absolute path completion routing rows define the adjacent input boundaries., Tests can use temp files, fake gateway RPC handlers, fake clipboard readers, fake paste-collapsers, and pure composer helpers; no live terminal, clipboard, image decoder, provider, or gateway process is required., This row owns input normalization and RPC envelopes only; native provider multimodal request routing stays in Phase 5.D/5.Q API-server multimodal rows. | - |
| 5 / 5.Q | API server multimodal content preservation | OpenAI-compatible chat-completions API server, Image input mode router + native content parts | OpenAI-compatible chat-completions API server is complete and already has content-normalization error envelopes., Image input mode router + native content parts is complete or fixture-ready so the turn loop can carry image parts beyond HTTP parsing., Tests can use httptest/fake turn loops and do not need live image downloads, providers, or network access. | Dashboard API client contract, Provider-tool-memory golden transcript suite, Release readiness e2e gate |
| 5 / 5.Q | API server legacy jobs routes + default toolset | API server cron admin read-only endpoints, API server cron admin mutating endpoints, Platform toolset config persistence + MCP sentinel | API server cron admin read-only and mutating endpoints are complete over native cron stores., Platform toolset config persistence + MCP sentinel and setup tools checklist rows are complete enough to resolve platform defaults and overrides., Tests can use fake cron stores, fake mutators, and in-memory toolset configs; no live scheduler, gateway adapter, provider, or platform credential is required. | Dashboard API client contract, Hermes API server operator parity, Release readiness e2e gate |
| 6 / 6.A | Hermes background review fork lifecycle | Background review toolset restriction, Native skills list/view tool surface, Hermes skill_manage support-file and curator intent actions | Background review toolset restriction is validated, so the review fork can be memory+skills-only from construction., Native skills list/view tool surface and Hermes skill_manage support-file and curator intent actions are validated, so the review fork can inspect and mutate skills without a Python subprocess., Tests can use fake review runners and fake stores; no live provider, Telegram, gateway, or shell command is required. | Hermes curator state transitions and run reports, Hermes curator command surface |
| 6 / 6.E | Hermes curator state transitions and run reports | Hermes background review fork lifecycle, Hermes curator auxiliary model routing slot, Hermes skill_manage support-file and curator intent actions, Portable SKILL.md format | Hermes background review fork lifecycle is builder-ready or validated so agent-created skills can be attributed to the review origin., Hermes skill_manage support-file and curator intent actions are validated, so curator consolidation can use write_file/remove_file, absorbed_into delete declarations, pinned guards, and background-review provenance rather than direct filesystem mutations., Portable SKILL.md format and native skill usage records exist, so curator state can be tested with local temporary skill roots., Tests can use fake clocks, fake skill roots, and fake review callbacks; no live LLM, gateway, or network call is required. | Hermes curator command surface, Skill effectiveness scoring |
| 6 / 6.F | Hermes skill_manage support-file and curator intent actions | Native skills list/view tool surface, Portable SKILL.md format | Native skills list/view tool surface is validated, so tests can create fixtures and inspect linked support files through Go-native tools., Portable SKILL.md format is validated, so supporting-file writes do not need to weaken SKILL.md frontmatter checks., The slice can use temporary skill roots, fake guard scanners, and fake background-review provenance; no live LLM, gateway, network, or real home directory access is required. | Hermes background review fork lifecycle, Hermes curator state transitions and run reports, Hermes curator command surface |
| 6 / 6.F | Hermes curator command surface | Hermes curator state transitions and run reports | Hermes curator state transitions and run reports is fixture-ready or validated, so the CLI can render real state and reports., The command registry still marks `curator` unavailable until this row ships, so the public surface remains honest. | - |
| 7 / 7.A | Signal transport/bootstrap layer | Inbound event normalization + session identity, Reply/send contract on shared chassis | Inbound event normalization + session identity and Reply/send contract on shared chassis are complete., Tests can inject fake health/RPC/SSE clients, fake clocks, and fake locks; no live signal-cli daemon, phone number registration, network socket, or attachment download is required., The bootstrap layer only adapts transport lifecycle into the existing Signal Bot contract. | Signal live transport smoke test, Voice attachment handling for Signal and QQ Bot, Channel health/status readout for paused adapters |
| 7 / 7.A | Signal markdown bodyRanges + attachment rate scheduler | Reply/send contract on shared chassis | Reply/send contract on shared chassis is complete, so formatted text and attachment scheduling can be modeled over existing Signal outbound decisions., Tests can use pure strings, fake clocks, and goroutine-controlled acquire calls; no live signal-cli, HTTP transport, phone registration, or media upload is required. | Signal transport/bootstrap layer, Signal live transport smoke test |
| 7 / 7.C | Matrix shared-chassis bot seam | Threaded text adapter contract suite | Threaded text adapter contract suite is complete., The slice can create internal/channels/matrix with pure structs and fake hook callbacks., Matrix self/bridge sender drop helper remains blocked until this seam exists, so the seam should expose a narrow place for sender filters to attach. | Matrix self/bridge sender drop helper, Matrix real client/bootstrap layer, Matrix E2EE device-id crypto-store binding |
| 7 / 7.C | Matrix self/bridge sender drop helper | Matrix shared-chassis bot seam | Matrix shared-chassis bot seam exists so the helper can live under internal/channels/matrix without inventing a parallel gateway adapter., The slice is pure string classification plus table tests; no live Matrix sync, login, pairing store, E2EE store, room join, or gateway manager behavior is required., Tests should cover sender localpart parsing with Matrix IDs only; no mautrix/nio SDK dependency is needed. | Matrix real client/bootstrap layer |
| 7 / 7.C | Mattermost shared-chassis bot seam | Threaded text adapter contract suite | Threaded text adapter contract suite is complete., The slice can create internal/channels/mattermost with pure event parser and fake delivery hooks., No REST/websocket client is required; tests inject double-encoded posted event JSON and fake post/upload responses. | Mattermost REST/WS bootstrap layer, Mattermost media upload contract |
| 7 / 7.C | Matrix real client/bootstrap layer | Matrix shared-chassis bot seam, Matrix self/bridge sender drop helper | Matrix shared-chassis bot seam is complete and exposes fakeable inbound/outbound hook points., Matrix self/bridge sender drop helper is complete or the bootstrap row wires only a placeholder sender-filter interface., Tests can drive a fake Matrix client with whoami/login/sync/send/upload methods; no Matrix SDK, homeserver, E2EE key store, or network is required. | Matrix E2EE device-id crypto-store binding, Matrix media upload/download contract, Matrix live transport binding |
| 7 / 7.C | Matrix E2EE device-id crypto-store binding | Matrix real client/bootstrap layer | Matrix real client/bootstrap layer has auth, sync/invite handling, room-kind policy, and a fakeable E2EE bootstrap seam. | - |
| 7 / 7.C | Mattermost REST/WS bootstrap layer | Mattermost shared-chassis bot seam | Mattermost shared-chassis bot seam is complete and exposes pure parser/reply-target behavior., Tests can drive fake REST and websocket clients with scripted responses; no Mattermost server, aiohttp equivalent, or network is required., Attachment evidence can reuse bounded artifact/path patterns instead of persisting arbitrary remote file names. | Mattermost live transport binding, Mattermost media upload contract |
| 7 / 7.E | Microsoft Teams adapter plugin seam | Plugin SDK, Gateway platform manifest + disabled regional adapter inventory | Shared gateway manager registration, plugin metadata loading, and regional adapter backlog patterns are validated., Tests can inject fake Teams app clients, fake activity objects, fake attachment cache functions, fake approval stores, and temp config/env values; no Microsoft Teams SDK, aiohttp, Bot Framework app, public tunnel, or network listener is required., This row only makes the platform visible as disabled/fakeable; live Bot Framework HTTP binding remains a later transport row. | - |
| 7 / 7.E | QQ Bot transport/bootstrap layer | QQ Bot shared-chassis bot seam | QQ Bot shared-chassis bot seam is complete., Tests can inject fake dependency probes, token clients, websocket frames, REST send clients, fake clocks, and fake locks; no QQ credential, aiohttp/httpx equivalent, live websocket, or media upload is required., The row remains a bootstrap/runtime seam and does not expand the QQ tool catalog. | QQ Bot live transport smoke test, Voice attachment handling for Signal and QQ Bot, Paused adapter channel health/status readout |
<!-- PROGRESS:END -->
