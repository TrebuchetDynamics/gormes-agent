interface:
  display_name: "Gormes Release"
  short_description: "Release Gormes through development, PR, tag, and CI."
  default_prompt: "Use $gormes-release to classify release intent, prepare the version and changelog on development, delegate green commit/PR work to $gormes-git, tag only a verified main commit, verify GitHub release artifacts, and stop on unsafe recovery conditions."
