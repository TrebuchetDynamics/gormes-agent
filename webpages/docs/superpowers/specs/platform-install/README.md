# Platform Install Specs

Owns installer parity, agent-template reset, onboarding wizard, first-run CLI/TUI UX, and native update command design decisions.

Exposes dated design specs for setup, update, and operator bootstrap flows.

Must never know about provider credentials, session data, or channel runtime internals except through documented setup contracts.
