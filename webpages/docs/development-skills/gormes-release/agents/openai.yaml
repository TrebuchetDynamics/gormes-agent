interface:
  display_name: "Gormes Release"
  short_description: "Release Gormes through PR, tag, and artifacts."
  default_prompt: "Use $gormes-release to run the development-to-main release lane: prepare version changes, validate, push development, merge the PR, tag, verify artifacts, and sync development."
