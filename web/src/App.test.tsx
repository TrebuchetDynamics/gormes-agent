// DashboardFrontendScaffold regression coverage lives in web/tests/run-tests.mjs so
// the row-local `npm --prefix web test -- --run DashboardFrontendScaffold` command
// stays hermetic before npm dependencies are installed. This file is the fixture
// anchor named by progress.json for the React/Vite scaffold slice.
