---
title: "Blocked Slices"
weight: 40
aliases:
  - /building-gormes/blocked-slices/
---

# Blocked Slices

This page is generated from canonical `progress.json` rows that declare
`blocked_by`.

Use it to avoid assigning work before the dependency chain is ready.

<!-- PROGRESS:START kind=blocked-slices -->
| Phase | Slice | Blocked by | Ready when | Unblocks |
|---|---|---|---|---|
| 2 / 2.B.11 | Discord forum media + polish parity | Discord forum channel ingress + thread lifecycle | Discord forum ingress and thread lifecycle fixtures are green on top of the shipped Discord adapter., The builder can use mocked Discord client/channel/thread/file types and existing testdata; no live Discord token, guild, upload, or network call is required., Gateway SessionSource metadata fixtures already preserve parent_chat_id, thread_id, and message_id. | Discord forum operator delivery polish, Channel directory refresh + stale-target invalidation |
| 2 / 2.F.4 | Channel directory atomic persistence + lookup | Home-channel ownership resolver | Home-channel resolver fixtures are green and explicit delivery-target parsing is stable., The slice can write to a temp state/home root in tests and must not inspect the real operator home directory., No live channel adapters are needed; fixture directories drive all lookup behavior. | Channel directory refresh + stale-target invalidation, Manager remember-source hook, Cron delivery target planner |
| 2 / 2.F.4 | Channel directory refresh + stale-target invalidation | Channel directory atomic persistence + lookup, Manager remember-source hook | Channel directory atomic persistence + lookup is complete and exposes a fakeable write/load API., Manager remember-source hook is complete or this slice limits itself to injected source fixtures and leaves Manager wiring for the blocked row., Refresh tests can inject adapter inventory snapshots and failures without live Slack/Discord clients. | Notify-to delivery routing, Discord forum media + polish parity, Cron delivery target planner |
| 4 / 4.A | Bedrock provider runtime binding | Bedrock Converse payload mapping (no AWS SDK), Bedrock stream event decoding (SSE fixtures), Bedrock SigV4 + credential seam, Bedrock stale-client eviction + retry classification | Bedrock Converse payload mapping (no AWS SDK), Bedrock stream event decoding (SSE fixtures), Bedrock SigV4 + credential seam, and Bedrock stale-client eviction + retry classification are complete., The builder can inject fake credentials, fake signer time, and fake Bedrock runtime responses; no AWS SDK, IMDS, profile file, or network access is required., Provider interface + stream fixture harness remains the only request/stream test surface. | Native runtime full-turn fixture |
| 4 / 4.A | Gemini Cloud Code request/stream mapper | Provider interface + stream fixture harness, Tool-call normalization + continuation contract | Provider interface + stream fixture harness can drive fake HTTP responses and shared stream events., Tool-call normalization + continuation contract is available so Gemini tool calls reuse the shared transcript shape., The slice stays pure mapper/client facade; Google project discovery and OAuth token refresh are supplied by fakes. | Google Code Assist project/quota resolver, Gemini Cloud Code provider transport |
| 4 / 4.A | OpenRouter compatible-provider routing | Provider interface + stream fixture harness, Tool-call normalization + continuation contract | Provider resolver tests can inject fake env/config and fake OpenAI-compatible transports., Shared provider error classifier accepts Retry-After and sanitized upstream bodies., Usage/pricing tests can load captured OpenRouter model metadata without network access. | Multi-model coordination, OpenRouter pricing cache refresh, OpenRouter provider health probe |
| 4 / 4.A | Google Code Assist project/quota resolver | Provider credential resolver seam | A Gormes token-provider interface exists or can be added under internal/hermes without binding to a real browser OAuth flow., Builder can drive all HTTP paths through httptest/fake RoundTripper and temp config/home directories., Gemini Cloud Code request mapper has a fake project/context seam to consume this resolver later. | Gemini Cloud Code provider transport, Google Code Assist OAuth login/import, Code Assist /gquota operator command |
| 4 / 4.B | Kernel compression-boundary callback binding | ContextEngine compression-boundary callback vocabulary, Context compression | ContextEngine compression-boundary callback vocabulary is complete on main., The worker identifies the existing kernel compression execution point; if no explicit compression execution path exists in the checkout, keep this row blocked and do not create fake wiring., A fake ContextEngine can record boundary notifications without calling a live provider or mutating real transcript storage. | - |
| 4 / 4.C | Toolset-aware skills prompt snapshot | Phase 2.G skills runtime, SKILL.md frontmatter validation guard | Phase 2.G skills runtime and SKILL.md frontmatter validation are stable enough to expose active skill snapshots and statuses., Tests can use t.TempDir skill roots, injected platform/disabled/toolset options, and fake snapshot paths., No model call, provider adapter, live plugin registry, semantic skill search, or skill installation flow is required. | Memory and session-search guidance assembly, Native full prompt assembly |
| 5 / 5.C | Browser provider bridge + Firecrawl fallback | Browser action contract, Browser hybrid private-URL local sidecar routing | Browser action contract and private-URL sidecar routing are stable enough to define a cloud-provider session result consumed by later chromedp/Rod runtime slices., Tests can inject fake HTTP transports for Browserbase and Firecrawl and synthetic credential structs; no DNS, Browserbase, Firecrawl, Chromium, chromedp, Rod, or Browser Use process is required., Provider selection remains optional and disabled when required credentials or project IDs are missing. | Chromedp, Rod, Browser action runtime provider selection, Browser provider status reporting |
| 5 / 5.I | Hindsight memory setup blank-input preservation | Third-party extensions | Third-party extensions have a native plugin config-patch seam; do not implement this before the generic plugin setup surface exists., Tests can exercise a pure config patch helper with in-memory maps and temp files; no Hindsight cloud/local daemon, uv install, Python plugin import, or Goncho memory change is required., This row is external memory-plugin compatibility only; internal Goncho remains the Go-native Honcho-compatible memory implementation. | - |
| 5 / 5.J | Approval mode config normalization | Hardline command pattern table + DetectHardline function, Recoverable dangerous patterns + blocked-result schema | Hardline command pattern table + DetectHardline function and Recoverable dangerous patterns + blocked-result schema are complete., The slice can table-test config values as plain Go types and call the existing pure GuardCommand path; no prompt UI, gateway slash command, cron runner, or smart LLM is required., Workers keep the parser reusable for later CLI/TUI/gateway config loaders. | Subagent dangerous-command non-interactive approval policy, Cron dangerous-command approval mode, Wire GuardCommand into execute_code boundary |
| 5 / 5.J | Subagent dangerous-command non-interactive approval policy | Recoverable dangerous patterns + blocked-result schema, Approval mode config normalization | Dangerous-command detection and approval-mode config normalization are fixture-locked for local tools. | - |
| 5 / 5.J | Concurrent tool approval callback propagation | Recoverable dangerous patterns + blocked-result schema, Subagent dangerous-command non-interactive approval policy | Recoverable dangerous patterns + blocked-result schema is validated, so approval decisions can reuse GuardCommand evidence., Subagent dangerous-command non-interactive approval policy is fixture-ready or validated; this row focuses on concurrent tool worker callback inheritance and fail-closed fallback., Tests use fake callbacks and fake worker functions only; no real terminal, prompt toolkit, shell command, or subagent backend is required. | Background review toolset restriction |
| 5 / 5.M | Multi-model coordination | OpenRouter compatible-provider routing, Debug helpers | OpenRouter compatible-provider routing is complete or the tool can inject a fake provider router and report moa_unavailable for missing live routing., Native tool registry can express requires_env/requires_provider metadata without enabling MOA by default., Tests can use fake child model outputs and fake clocks; no OPENROUTER_API_KEY or network is required. | Agentic model/tool features, Provider fan-out policy controls |
| 5 / 5.N | Debug share paste sweep scheduler contract | Debug helpers | Debug helpers are split into a Go-native diagnostic surface with redaction and no hidden writes., A native debug-share pending-paste queue exists or is specified as a pure helper before scheduler wiring begins., Tests can use fake clock and fake deleter; no network DELETE, paste.rs upload, gateway process, or live cron daemon is required. | - |
| 5 / 5.O | Hermes CLI nested parser inventory refresh | Hermes CLI command-tree parity manifest | Hermes CLI command-tree parity manifest is complete and can be refined without changing runtime command handlers., The builder can assert the nested parser inventory with in-process Go fixtures and static upstream source refs; no provider credentials, gateway service, browser, network, or plugin install is required., The Hugo parity matrix already records the current upstream nested parser inventory and known manifest drift. | Hermes auth command-tree manifest refresh, Gateway, platform, webhook, and cron management CLI, Diagnostics, backup, logs, and status CLI, Gormes config command surface, Gormes model interactive provider/model picker, Gormes mcp login OAuth re-auth bridge, Plugin SDK, Tool/runtime/security rows, Session shutdown memory transcript handoff |
| 5 / 5.O | Hermes auth OAuth provider adapters | Hermes auth command-tree manifest refresh, Hermes auth credential-pool command surface, Anthropic OAuth/keychain credential discovery, Codex OAuth state + stale-token relogin, Multi-account auth, Google OAuth flow + refresh seam | Hermes auth credential-pool command surface is fixture-ready or complete so command routing, storage, redaction, and suppression semantics exist., Codex OAuth state + stale-token relogin, Anthropic OAuth/keychain credential discovery, Multi-account auth, and Google OAuth flow + refresh seam are complete enough to provide native stores or fake login seams., Tests can inject fake OAuth/device-code/Qwen clients and temp auth stores; no browser, localhost callback, keychain, Qwen CLI, Google OAuth server, Codex endpoint, Nous portal, or Anthropic network is required. | Provider auth selection for native gateway turns, Qwen OAuth provider runtime binding, Google Gemini CLI provider runtime binding, Nous provider auth refresh and agent-key recovery |
| 5 / 5.O | Hermes auth Spotify service-provider subcommand | Hermes auth command-tree manifest refresh, Hermes auth credential-pool command surface, First-party Spotify plugin metadata fixture | First-party Spotify plugin metadata fixture is complete and already gates capabilities on providers.spotify., Hermes auth command-tree manifest refresh and credential-pool command surface are planned or complete., Tests can inject PKCE state, callback input, token exchange responses, fake clock, and temp auth stores; no Spotify account, browser, localhost socket, or network access is required. | Spotify plugin backend runtime, Spotify tool handlers over providers.spotify auth |
| 5 / 5.O | Gormes auth bare interactive credential-pool readout | Hermes auth command-tree manifest refresh, Hermes auth credential-pool command surface | Hermes auth command-tree manifest refresh is validated, so `auth` parser shape is row-classified., Hermes auth credential-pool command surface row exposes redacted list/format helpers the bare readout can reuse., Tests inject a fake stdout writer and a fake AWS identity probe; no live boto3, AWS network, browser, keychain, or interactive TTY is required. | Gormes auth status per-provider aggregator, Gateway, platform, webhook, and cron management CLI |
| 5 / 5.O | Gormes auth status per-provider aggregator | Hermes auth credential-pool command surface, Multi-account auth, Anthropic OAuth/keychain credential discovery, Codex OAuth state + stale-token relogin, Google OAuth flow + refresh seam | Hermes auth credential-pool command surface row provides shared list/redaction helpers., Backend rows for token vault (11069), multi-account auth (11206), Anthropic OAuth/keychain (11136), Codex OAuth state (8908), Google OAuth (11279), and Bedrock SigV4 (8345) are complete and expose redacted read models., Tests use injected fake auth-state stores and temp XDG dirs; no live keychain, browser, OAuth refresh, or provider network call is required. | Gormes setup minimal sectioned wizard slice, Diagnostics, backup, logs, and status CLI |
| 5 / 5.O | Gormes auth add openai-codex strict isolation contract | Hermes auth OAuth provider adapters, Codex OAuth state + stale-token relogin | Hermes auth OAuth provider adapters row provides the injectable Codex device-code seam., Codex OAuth state + stale-token relogin row exposes the Gormes-owned Codex auth.json store., Tests can inject a fake device-code transport, a fake `~/.codex/auth.json` shadow file, and a fake clock; no live ChatGPT/Codex backend is required. | Gormes model interactive provider/model picker |
| 5 / 5.O | Gormes top-level logout provider shortcut | Hermes auth credential-pool command surface, Hermes auth command-tree manifest refresh | Hermes auth credential-pool command surface row exposes the underlying logout helper used by `auth logout`., Hermes auth command-tree manifest refresh classifies top-level `logout` as a non-deprecated provider-auth shortcut., Tests inject a temp auth.json store and assert provider scoping; no live keychain, browser, or network access is required. | Gormes login deprecated-redirect contract |
| 5 / 5.O | Gormes login deprecated-redirect contract | Hermes auth command-tree manifest refresh, Gormes top-level logout provider shortcut | Hermes auth command-tree manifest refresh classifies top-level `login` as removed/deprecated guidance., Gormes top-level logout provider shortcut row defines the parallelism between deprecated `login` and live `logout`., Tests can capture stdout/stderr from a Cobra invocation; no live OAuth or provider network is required. | Gormes model interactive provider/model picker |
| 5 / 5.O | Gormes model interactive provider/model picker | Provider endpoint/API-key root flags + runtime resolution, TUI launch model override + static alias resolver, Hermes config.yaml model/provider runtime bridge, Hermes auth OAuth provider adapters, Gormes auth add openai-codex strict isolation contract | Provider endpoint/API-key root flags + runtime resolution row exposes the runtime model/provider override path., TUI launch model override + static alias resolver row is shipped so the picker can reuse alias resolution., Hermes config.yaml model/provider runtime bridge row provides the persistence target (model.default, model.provider)., Hermes auth OAuth provider adapters and Gormes auth add openai-codex strict isolation contract are planned or complete so the picker delegates rather than re-implementing OAuth., Gateway `_handle_model_command` overlap is coordinated with the Gateway management CLI umbrella row so the same handler is not rowed twice. | Gormes setup minimal sectioned wizard slice, Gateway, platform, webhook, and cron management CLI |
| 5 / 5.O | Gormes setup minimal sectioned wizard slice | Gormes model interactive provider/model picker, Gormes config command surface | Gormes model interactive provider/model picker row exposes the shared picker entry point so the setup model section delegates rather than re-implementing., Gormes config command surface is complete so config-mutation paths exist for the model section to persist into., Tests can inject a fake TTY/menu seam and a fake config writer; no live provider, browser, or terminal probing is required. | Gormes uninstall dry-run command contract, Hermes config migration dry-run manifest |
| 5 / 5.O | Gormes uninstall dry-run command contract | Gormes config command surface, Hermes auth credential-pool command surface | Gormes config command surface is complete so the canonical config and dotenv paths are addressable., Hermes auth credential-pool command surface row exposes the credential pool path resolver., Tests use temp HOME and temp XDG dirs only; no real user state, root-owned paths, or sudo are touched. | Hermes config migration dry-run manifest, OpenClaw migration dry-run manifest |
| 5 / 5.O | Gormes mcp login OAuth re-auth bridge | MCP OAuth state store + noninteractive auth errors, MCP OAuth refresh + 401 session-expired recovery | MCP OAuth state store + noninteractive auth errors row exposes the persisted state and remove helper., MCP OAuth refresh + 401 session-expired recovery row exposes the OAuth-refresh seam the relogin command can drive., Tests inject a fake MCP config loader, fake OAuth refresh transport, and fake clock; no live MCP server, browser callback, or localhost socket is required., Open question (see acceptance below): planner+operator decide whether the initial slice ships a fake-callback fixture or stays noninteractive-only. | Diagnostics, backup, logs, and status CLI |
| 5 / 5.P | Unix installer root/FHS layout policy | Unix installer (install.sh) source-backed update flow | Unix installer (install.sh) source-backed update flow has canonical scripts under scripts/ and a byte-equal served site copy. | Installer site asset/route coverage |
| 6 / 6.B | LLM-assisted pattern distillation | Portable SKILL.md format | Portable SKILL.md format is validated, so generated drafts can carry review state and remain out of prompt injection until approved., The row can use fake model/review prompt fixtures; no live provider, skill install, or prompt injection is required. | - |
| 6 / 6.C | Portable SKILL.md format | SKILL.md frontmatter validation guard, Hermes creative skill metadata compatibility, Phase 2.G skills runtime | SKILL.md frontmatter validation guard is validated so malformed skill files are excluded before review metadata is interpreted., Hermes creative skill metadata compatibility is validated so versioned metadata preserves real upstream skill routing rather than an invented generic design category., Phase 2.G skills runtime is complete and the parser/store seam is stable enough for versioned metadata. | LLM-assisted pattern distillation, Hybrid lexical + semantic lookup, Skill effectiveness scoring |
| 6 / 6.D | Hybrid lexical + semantic lookup | Portable SKILL.md format | Portable SKILL.md format is validated so skills carry review state, triggers, exclusions, and provenance metadata., Phase 3 semantic embedding lookup remains optional and can be stubbed in unit fixtures without Ollama. | Source-aware retrieval damping fixtures, Code Cathedral II code-context retrieval fixtures, Skill effectiveness scoring |
| 6 / 6.D | Source-aware retrieval damping fixtures | Hybrid lexical + semantic lookup | Hybrid lexical + semantic lookup is validated and returns score explanations that can carry source-tier evidence., The worker can use synthetic skill/memory sources and fake scores; no GBrain SQL engine, PGLite, Postgres, or tree-sitter runtime is required. | Delta-bounded skill and memory maintenance passes, Code Cathedral II code-context retrieval fixtures, Skill effectiveness scoring |
| 6 / 6.D | Delta-bounded skill and memory maintenance passes | Source-aware retrieval damping fixtures | Hybrid lexical + semantic lookup and Source-aware retrieval damping fixtures are validated so maintenance evidence can reuse score explanations and source tiers., The worker can use fake skill/memory rows plus injected source-anchor and stale-count callbacks; no live embeddings, Ollama, Postgres/PGLite, git cat-file, or tree-sitter runtime is required. | Skill effectiveness scoring |
| 6 / 6.D | Code Cathedral II code-context retrieval fixtures | Hybrid lexical + semantic lookup | Hybrid lexical + semantic lookup is validated and can accept optional external evidence in fixtures. | - |
| 7 / 7.A | Signal transport/bootstrap layer | Inbound event normalization + session identity, Reply/send contract on shared chassis | Inbound event normalization + session identity and Reply/send contract on shared chassis are complete., Tests can inject fake health/RPC/SSE clients, fake clocks, and fake locks; no live signal-cli daemon, phone number registration, network socket, or attachment download is required., The bootstrap layer only adapts transport lifecycle into the existing Signal Bot contract. | Signal live transport smoke test, Voice attachment handling for Signal and QQ Bot, Channel health/status readout for paused adapters |
| 7 / 7.C | Matrix shared-chassis bot seam | Threaded text adapter contract suite | Threaded text adapter contract suite is complete., The slice can create internal/channels/matrix with pure structs and fake hook callbacks., Matrix self/bridge sender drop helper remains blocked until this seam exists, so the seam should expose a narrow place for sender filters to attach. | Matrix self/bridge sender drop helper, Matrix real client/bootstrap layer, Matrix E2EE device-id crypto-store binding |
| 7 / 7.C | Matrix self/bridge sender drop helper | Matrix shared-chassis bot seam | Matrix shared-chassis bot seam exists so the helper can live under internal/channels/matrix without inventing a parallel gateway adapter., The slice is pure string classification plus table tests; no live Matrix sync, login, pairing store, E2EE store, room join, or gateway manager behavior is required., Tests should cover sender localpart parsing with Matrix IDs only; no mautrix/nio SDK dependency is needed. | Matrix real client/bootstrap layer |
| 7 / 7.C | Mattermost shared-chassis bot seam | Threaded text adapter contract suite | Threaded text adapter contract suite is complete., The slice can create internal/channels/mattermost with pure event parser and fake delivery hooks., No REST/websocket client is required; tests inject double-encoded posted event JSON and fake post/upload responses. | Mattermost REST/WS bootstrap layer, Mattermost media upload contract |
| 7 / 7.C | Matrix real client/bootstrap layer | Matrix shared-chassis bot seam, Matrix self/bridge sender drop helper | Matrix shared-chassis bot seam is complete and exposes fakeable inbound/outbound hook points., Matrix self/bridge sender drop helper is complete or the bootstrap row wires only a placeholder sender-filter interface., Tests can drive a fake Matrix client with whoami/login/sync/send/upload methods; no Matrix SDK, homeserver, E2EE key store, or network is required. | Matrix E2EE device-id crypto-store binding, Matrix media upload/download contract, Matrix live transport binding |
| 7 / 7.C | Matrix E2EE device-id crypto-store binding | Matrix real client/bootstrap layer | Matrix real client/bootstrap layer has auth, sync/invite handling, room-kind policy, and a fakeable E2EE bootstrap seam. | - |
| 7 / 7.C | Mattermost REST/WS bootstrap layer | Mattermost shared-chassis bot seam | Mattermost shared-chassis bot seam is complete and exposes pure parser/reply-target behavior., Tests can drive fake REST and websocket clients with scripted responses; no Mattermost server, aiohttp equivalent, or network is required., Attachment evidence can reuse bounded artifact/path patterns instead of persisting arbitrary remote file names. | Mattermost live transport binding, Mattermost media upload contract |
| 7 / 7.E | Feishu drive-comment rule + pairing seam | Feishu transport/bootstrap layer | Feishu transport/bootstrap layer is planned or complete enough to provide a channel package and temp-root config paths., Tests can inject rules JSON and pairing JSON under t.TempDir with fake mtime/clock behavior., No Feishu SDK, Drive API, or comment reply workflow is required. | Feishu drive-comment reply workflow |
| 7 / 7.E | Feishu drive-comment reply workflow | Feishu drive-comment rule + pairing seam, Feishu transport/bootstrap layer | Feishu transport/bootstrap layer and Feishu drive-comment rule + pairing seam are complete enough to provide fake event and access-control inputs., Tests can inject fake Drive/comment/reaction clients, fake agent invoker, fake wiki lookup, and deterministic comment fixtures., No live Feishu credentials, websocket, HTTP server, Drive API, or model/provider call is required. | Feishu live SDK binding, Native gateway agent invocation for document comments |
| 7 / 7.E | QQ Bot transport/bootstrap layer | QQ Bot shared-chassis bot seam | QQ Bot shared-chassis bot seam is complete., Tests can inject fake dependency probes, token clients, websocket frames, REST send clients, fake clocks, and fake locks; no QQ credential, aiohttp/httpx equivalent, live websocket, or media upload is required., The row remains a bootstrap/runtime seam and does not expand the QQ tool catalog. | QQ Bot live transport smoke test, Voice attachment handling for Signal and QQ Bot, Paused adapter channel health/status readout |
<!-- PROGRESS:END -->
